# 練習程式已寫出完整

以下檔案的主要練習區 Herodoc（或等價區塊）**皆為實際可執行程式**，非占位。

| 檔案 | 說明 |
|------|------|
| `01-basic.php` | 兩段：變數輸出、兩數對調，Herodoc 內皆完整。 |
| `02-select.php` | 兩張卡：`if` 及格判定、`switch` 評語，程式區皆完整。 |
| `03-forloop.php` | 多個 for／while／foreach 區塊，每段皆為完整迴圈程式。 |
| `04-pra01.php` | 成績等級 `if / else if` 整段邏輯已寫滿。 |
| `07-pra04.php` | 兩種九九乘法表（一般表、交叉表）雙層迴圈與表格輸出已寫。 |
| `10-array.php` | 多維陣列 + 雙層 `foreach` 輸出 `<table>`，與下方執行區一致。 |
| `11-pra07.php` | 兩段：建 `$nine` 陣列、`foreach` 組表格，皆完整。 |

**共 7 個檔案。**
