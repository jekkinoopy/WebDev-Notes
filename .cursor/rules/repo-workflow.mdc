---
description: 本 repo 協作慣例（時區、還原、索引、HTML 筆記）
globs: "*"
alwaysApply: true
---

# WebDev-Notes 協作慣例

## 時區與「昨天／今天」

- 以**使用者所在地（預設台灣 UTC+8）**理解「昨天、今天、剛剛」。
- **不可**只憑 `git log` 的 commit 日期就認定「昨天」並 `reset`。
- 使用者說「還原到昨天／弄壞前」時：先 `git log` / `reflog` 列出候選 commit，**確認要回到哪一個 SHA** 再動手；未 push 時以**本地 HEAD／reflog** 為準。

## Git 還原

- 禁止未確認就 `git reset --hard`。
- 還原後若本地落後遠端，**提醒勿盲目 `git pull`**（會拉回不要的提交）；若要改遠端須使用者同意後再 `push --force`。

## PHP 課程索引 `courses/01-database/php/index.html`

- **新筆記卡片一律加在 `notes-grid` 最後**（既有單元之後），不要插在最前面。

## PHP 單元：先判斷「練習」還是「講義」（死線，勿搞混）

使用者說「新增筆記／練習／作業」且題目要自己寫程式時，**預設＝練習單元**，不是講義靜態頁。

| 類型 | 主檔 | 對照範本 | `index` 卡片連 |
|------|------|----------|----------------|
| **練習單元** | **`.php`**（`h*.php`、`*-pra*.php`） | **`h04-bmi.php`**、**`h01-string.php`** | **`.php`** |
| 講義整理（概念＋示範已完成） | `.html` 或已示範的 `.php` | `01-basic.html`、`03-forloop.html` | 已確認 `.html` 者連 `.html` |

### 練習單元：禁止代寫答案（助理常犯，使用者不必再提醒）

- **只留【題目需求】＋ `// 【程式碼練習】` 骨架**（空函式、`// 在此實作`、依題目拆的註解步驟即可）。
- **【執行結果】**區只留 `<?php // 完成後在此 ?>` 或空白；**不得**預先 `echo` 出正確圖形／正確輸出。
- **不得**在程式碼窗貼上可一鍵跑通的完整解法（含 `for` + `str_repeat` 等全套）。
- **不得**因「方便預覽」就建一份含答案的 `.html`；練習只有 `.php` 一份即可。
- 選做／延伸（菱形、方形）只寫在【題目需求】一句，**不要**順手寫好函式。
- 新增後：`note-lesson-nav.js` 的 `CHAIN` 加一筆；**不要**加進 `PHP_LESSON_STATIC_HTML`（除非日後使用者明確要轉講義 `.html`）。

### 講義靜態 `.html`（與練習分開）

- 僅在：**使用者明確要轉 `.html`**，或該單元本來就是講義示範（如 `01-basic.html`）時才做。
- 依 `01-basic.html` 等做法轉出；`index`、導覽改連 `.html`；並在 `PHP_LESSON_STATIC_HTML` 登記 `id`。
- **不得**把「新增練習」預設成先建 `.html` 講義。

## 筆記內容

- 【學習重點】**不要**加「下一步：請進入 xxx」；頁底 `note-lesson-nav` 已負責上下則。
- 需求區塊沿用 repo 慣例：**【題目需求】**（`ques-section`），勿自創【學習段落】除非使用者指定。
- 與 `CURSOR-HANDOFF.md`「課中」一致：**程式區先留白，練完再補**執行結果／學習重點（助理新建練習時也要遵守）。

## conan-school（已搬出本 repo）

- **勿**在 `courses/07-projects/practice/conan-school/` 新增辦案筆記、改練習 PHP/HTML、或維護 `conan-notes-nav.js`（本目錄僅留轉址與 README）。
- **對外連結一律**：<https://jekkinoopy.github.io/conan-school/>（辦案筆記：<https://jekkinoopy.github.io/conan-school/notes/index.html>）。
- **原始碼**：[jekkinoopy/conan-school](https://github.com/jekkinoopy/conan-school)；辦案筆記三處同步等規則在該 repo 內執行。
- WebDev-Notes 內已改連外部站：`practice/index.html`、`01-database/php/index.html`、`practice-peer-nav.js` 等。

## 獨立專案搬移

- 專案搬出本 repo 時，將 **`docs/portable-collab-rules/`** 整包複製到新專案根目錄（含 `.cursor/rules`、`CURSOR-HANDOFF.project.md` → 更名 `CURSOR-HANDOFF.md` 並改專案名）。

## 決策

- 有 repo 慣例就照做；**使用者明確要求**的優先於助理自判「不必」。
