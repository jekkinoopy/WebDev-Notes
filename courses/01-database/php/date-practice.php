<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>日期函式應用練習 - 努比的全端筆記</title>
    <link rel="stylesheet" href="../../../assets/css/main.css">
    <link rel="stylesheet" href="../../../assets/css/course-note.css">
</head>
<body>
    <header class="header">
        <nav class="navbar"></nav>
        <script src="../../../assets/js/nav-loader.js"></script>
    </header>

    <section class="page-hero">
        <div class="hero-container">
            <span class="category-tag">PHP 基礎</span>
            <h2 class="note-title">日期函式應用練習</h2>
            <p class="hero-desc">實作給定日期計算間隔天數及生日倒數計算。</p>
            <div class="hero-divider"></div>
        </div>
    </section>

    <div class="note-container">
        <!-- 練習一 -->
        <div class="note-card">
            <h3 class="note-subtitle">計算兩個日期之間隔天數</h3>
            <ul class="custom-list">
                <li>給定兩個日期字串，轉換為時間戳進行運算。</li>
            </ul>
            <pre><code><?php
// 使用 Heredoc (<<<EOD) 定義字串，這樣裡面放單引號或雙引號都不會出錯
$code = <<<'EOD'
// 在此處實作練習邏輯
EOD;
echo htmlspecialchars($code);
?></code></pre>
            <div class="code-section">
                <?php
                    // 執行結果預留位置
                ?>
            </div>
        </div>

        <!-- 練習二 -->
        <div class="note-card" style="margin-top: 30px;">
            <h3 class="note-subtitle">計算距離下次生日天數</h3>
            <ul class="custom-list">
                <li>以當前日期為準，計算距離下一次生日剩餘天數。</li>
            </ul>
            <pre><code><?php
// 使用 Heredoc (<<<EOD) 定義字串，這樣裡面放單引號或雙引號都不會出錯
$code = <<<'EOD'
// 在此處實作練習邏輯
EOD;
echo htmlspecialchars($code);
?></code></pre>
            <div class="code-section">
                <?php
                    // 執行結果預留位置
                ?>
            </div>
        </div>
    </div>
</body>
</html>